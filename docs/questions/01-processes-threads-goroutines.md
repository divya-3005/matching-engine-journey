# Processes, Threads and Goroutines

## Questions

1. Why does the output order change?

2. Why doesn't worker() block main()?

3. What does the `go` keyword actually do?

4. Who decides which goroutine runs first?

5. Are two CPU cores required for two goroutines?

6. What would happen if we removed time.Sleep() from main()?

7. Does every goroutine create an operating system thread?

8. Why are goroutines cheaper than threads?